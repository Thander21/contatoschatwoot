name: contatoschatwoot
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  excel: ^2.0.0-null-safety-3
  path_provider: ^2.0.2
  cupertino_icons: ^1.0.8
  window_manager: ^0.3.0
  logging: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
